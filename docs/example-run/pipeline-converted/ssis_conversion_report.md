# SSIS Conversion Report

> **Generated:** 2026-06-30 04:57 UTC  
> **Package:** DailyETLMain  
> **Tasks assessed:** 95

---

## Connection Managers

| Source Connection | Type | Target | Rationale |
|---|---|---|---|
| WWI_Source_DB | OLEDB | Lakehouse Federation connection (or JDBC + Databricks secret scope) | Source OLTP system remains external; credentials must never be hardcoded in notebooks — use a Databricks secret scope referenced from a Unity Catalog connection (Lakehouse Federation) or directly in the Spark JDBC read options. |
| WWI_DW_Destination_DB | OLEDB | Unity Catalog (no connection object needed) | The destination DW becomes the Databricks workspace itself — writes go directly to Delta tables in the wwi_<env> catalog via Spark/Databricks SQL, replacing the OLE DB destination connection entirely. |

## Package Variables

| Source Variable | Namespace | Sample Value | Target |
|---|---|---|---|
| LastETLCutoffTime | User | 4/11/2016 10:39:10 AM | ops.etl_watermark Delta table column (read/written by the orchestration task, not a job parameter — must persist across runs) |
| LineageKey | User | 0 | Computed value inside the MERGE task (MAX(lineage_key)+1 or IDENTITY column) — not passed as a parameter |
| TableName | User | None | Workflow job parameter `entity_name`, templated into each per-entity job run |
| TargetETLCutoffTime | User | 4/13/2016 11:25:23 AM | ops.etl_watermark Delta table column (read/written by the orchestration task, not a job parameter — must persist across runs) |

### Shared-Variable Scoping Hazards

Variables written by more than one task — safe only under strictly sequential execution. See target_state_architecture.md's parallelization recommendation before assuming these are safe in the target design.

| Variable | Writer Count | Writer Tasks | Risk |
|---|---|---|---|
| TargetETLCutoffTime | 2 | Calculate ETL Cutoff Time backup, Trim Any Milliseconds | Package-scoped variable written by 2 different tasks. Safe only under SSIS's default sequential execution. If the target Workflow design parallelises these tasks' entity loads, this MUST be mapped to a per-entity-scoped value (e.g. a Workflow task parameter or job-run-scoped variable), never a single shared value read across concurrently-running tasks. |
| TableName | 13 | Set TableName to City, Set TableName to Customer, Set TableName to Employee (+10 more) | Package-scoped variable written by 13 different tasks. Safe only under SSIS's default sequential execution. If the target Workflow design parallelises these tasks' entity loads, this MUST be mapped to a per-entity-scoped value (e.g. a Workflow task parameter or job-run-scoped variable), never a single shared value read across concurrently-running tasks. |

---

## Per-Task Conversion Detail

Every SSIS task in the package, with original metadata, target mapping, conversion confidence (0–1), and recommended test depth.

### SSIS_PACKAGE (1)

| Task | Original Metadata | Target Mapping | Confidence | Test Recommendation |
|---|---|---|---|---|
| DailyETLMain | Top-level package | Databricks Workflow job | 0.95 | Full end-to-end parallel run against a copy of production data for at least one complete daily cycle before cutover. |

### SSIS_SEQUENCE_CONTAINER (13)

| Task | Original Metadata | Target Mapping | Confidence | Test Recommendation |
|---|---|---|---|---|
| Load City Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Customer Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Employee Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Movement Fact | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Order Fact | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Payment Method Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Purchase Fact | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Sale Fact | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Stock Holding Fact | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Stock Item Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Supplier Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Transaction Fact | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |
| Load Transaction Type Dimension | 0 inbound precedence constraint(s) within container | Workflow task group (dependency-chained tasks, no container object needed) | 0.90 | Integration test: run the full per-entity task chain end-to-end against a test catalog and confirm final row counts match a known-good baseline. |

### SSIS_EXECUTE_SQL (53)

| Task | Original Metadata | Target Mapping | Confidence | Test Recommendation |
|---|---|---|---|---|
| Ensure Date Dimension includes current year | DECLARE @YearNumber int =  YEAR(SYSUTCDATETIME());  EXEC Integration.PopulateDat... | Databricks SQL task (best-effort syntax translation required) | 0.65 | Manual test plan required. |
| Get Last City ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Customer ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Employee ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Movement ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Movement ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Movement ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Movement ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Movement ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Movement ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Payment Method ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Stock Item ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Supplier ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Last Transaction Type ETL Cutoff Time | EXEC Integration.GetLastETLCutoffTime ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Unit test the watermark read/advance logic with boundary cases: first run (no prior watermark), normal incremental run, and a run with zero new rows. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Get Lineage Key | EXEC Integration.GetLineageKey ?, ?; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Verify the computed lineage key is unique and monotonically increasing across consecutive runs. |
| Migrate Staged City Data | EXEC Integration.MigrateStagedCityData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Customer Data | EXEC Integration.MigrateStagedCustomerData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Employee Data | EXEC Integration.MigrateStagedEmployeeData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Movement Data | EXEC Integration.MigrateStagedMovementData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Order Data | EXEC Integration.MigrateStagedOrderData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Payment Method Data | EXEC Integration.MigrateStagedPaymentMethodData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Purchase Data | EXEC Integration.MigrateStagedPurchaseData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Sale Data | EXEC Integration.MigrateStagedSaleData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Stock Holding Data | EXEC Integration.MigrateStagedStockHoldingData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Stock Item Data | EXEC Integration.MigrateStagedStockItemData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Supplier Data | EXEC Integration.MigrateStagedSupplierData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Transaction Data | EXEC Integration.MigrateStagedTransactionData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Migrate Staged Transaction Type Data | EXEC Integration.MigrateStagedTransactionTypeData; | Databricks SQL task invoking the converted SQL logic (see output/databricks_sql) or a Python task calling the converted PySpark function | 0.75 | Row-level reconciliation between the staging data and the resulting Silver/Gold table after MERGE — confirm SCD2 versioning (Valid From/To) is correctly applied for both new and changed rows. |
| Truncate City_Staging | DELETE FROM Integration.City_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Customer_Staging | DELETE FROM Integration.Customer_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Employee_Staging | DELETE FROM Integration.Employee_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Movement_Staging | DELETE FROM Integration.Movement_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Order_Staging | DELETE FROM Integration.Order_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate PaymentMethod_Staging | DELETE FROM Integration.PaymentMethod_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Purchase_Staging | DELETE FROM Integration.Order_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Sale_Staging | DELETE FROM Integration.Sale_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate StockHolding_Staging | DELETE FROM Integration.StockHolding_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate StockItem_Staging | DELETE FROM Integration.StockItem_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Supplier_Staging | DELETE FROM Integration.Supplier_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate TransactionType_Staging | DELETE FROM Integration.TransactionType_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |
| Truncate Transaction_Staging | DELETE FROM Integration.Transaction_Staging; | Databricks SQL task (DELETE) — superseded by Delta overwrite-mode write in the collapsed design | 0.95 | Verify the target staging table is empty (or correctly overwritten) immediately before each extract task runs. |

### SSIS_DATA_FLOW (13)

| Task | Original Metadata | Target Mapping | Confidence | Test Recommendation |
|---|---|---|---|---|
| Extract All Stock Holding Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated City Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Customer Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Employee Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Movement Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Order Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Payment Method Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Purchase Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Sale Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Stock Item Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Supplier Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Transaction Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |
| Extract Updated Transaction Type Data to Staging | 2 pipeline component(s) | PySpark extract task (JDBC read -> Delta append/overwrite write) | 0.85 | Row-count parity check between source query result and the written Bronze staging Delta table, plus a column-level checksum on a sampled subset, run once against a representative incremental window. |

### SSIS_EXPRESSION (15)

| Task | Original Metadata | Target Mapping | Confidence | Test Recommendation |
|---|---|---|---|---|
| Calculate ETL Cutoff Time backup | @[User::TargetETLCutoffTime] = DATEADD("Minute", -5, GETUTCDATE()  ) | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to City | @[User::TableName] = "City" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Customer | @[User::TableName] = "Customer" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Employee | @[User::TableName] = "Employee" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Movement | @[User::TableName] = "Movement" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Order | @[User::TableName] = "Order" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Payment Method | @[User::TableName] = "Payment Method" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Purchase | @[User::TableName] = "Purchase" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Sale | @[User::TableName] = "Sale" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Stock Holding | @[User::TableName] = "Stock Holding" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Stock Item | @[User::TableName] = "Stock Item" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Supplier | @[User::TableName] = "Supplier" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Transaction | @[User::TableName] = "Transaction" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Set TableName to Transaction Type | @[User::TableName] = "Transaction Type" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |
| Trim Any Milliseconds | @[User::TargetETLCutoffTime] = DATEADD("Millisecond", 0 - DATEPART("Millisecond" | Workflow task parameter computed in a Python task (no native expression language in Workflows) | 0.55 | Unit test the Python equivalent of the expression against the same input values used in the original SSIS expression, including edge cases (null/empty TableName, DST boundary for GETUTCDATE-based cutoffs). |

---

## Summary

- **Average conversion confidence across all 95 tasks:** 0.78
- **Tasks below 0.7 confidence (recommend manual review):** 16

---

_Granular 1:1 task mapping is shown above for full traceability. For production implementation, consider collapsing the recurring 5-step per-entity pattern into 3 Workflow tasks as recommended in `target_state_architecture.md` / `orchestration_design.md` — this report intentionally preserves task-level fidelity so every original SSIS task can be individually audited._