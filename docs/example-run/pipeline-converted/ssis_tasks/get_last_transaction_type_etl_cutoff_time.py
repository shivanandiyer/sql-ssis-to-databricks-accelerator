# Converted from SSIS Execute SQL Task: Get Last Transaction Type ETL Cutoff Time
# Original: EXEC Integration.GetLastETLCutoffTime ?;
# Calls converted procedure: Integration.GetLastETLCutoffTime — see output/databricks_sql or output/pyspark
#  for that procedure's converted logic (from the SQL conversion layer).

def run(*args, **kwargs) -> None:
    """Invoke the converted logic for Integration.GetLastETLCutoffTime."""
    raise NotImplementedError('Wire up a call to the converted procedure logic.')