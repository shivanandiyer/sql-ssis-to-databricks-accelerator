# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Test / lint / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Accelerator pipeline outputs — regenerated by run_analysis.py,
# run_impact_analysis.py, run_target_state_design.py, run_conversion.py,
# and run_ssis_conversion.py. Never commit these; they are large, derived,
# and reproducible from the source corpus + accelerator code alone.
output/
outputs/

# Local environment / secrets — see conf/secrets.md for how secrets are
# actually referenced (Databricks secret scopes), never committed here.
.env
.env.*
*.pem
*.key
secrets.yml
secrets.yaml

# Databricks Asset Bundle local state
bundle/.databricks/
.databricks/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
