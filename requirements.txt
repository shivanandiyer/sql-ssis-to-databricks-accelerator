# Full dependency list for running the MCP server, the accelerator pipeline,
# and the test suite. The core analysis/conversion pipeline itself is pure
# Python standard library (see pyproject.toml's "dependencies = []" and the
# commentary there), but the MCP server layer and optional pipeline features
# require the packages below.

# MCP server (mcp/server.py, mcp/tools/*.py)
mcp>=1.0.0

# Optional pipeline enhancements (future converters / parsers may use these)
pyyaml>=6.0
networkx>=3.0
lxml>=5.0
sqlglot>=25.0

# CLI entrypoint (accelerator/cli.py, when implemented)
click>=8.1.0

# Test suite
pytest>=8.0
pytest-asyncio>=0.23.0
